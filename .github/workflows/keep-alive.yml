name: Keep Render Instance Alive

on:
  schedule:
    - cron: '*/10 * * * *' # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping Endpoint
        run: |
          curl -s -o /dev/null -w "%{http_code}" ${{ secrets.KEEP_ALIVE_URL }}